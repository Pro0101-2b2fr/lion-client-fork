interface:
  display_name: "Lion Client Fork"
  short_description: "Repo-specific patterns and workflows for lion-client-fork"
  default_prompt: "Use the lion-client-fork repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true